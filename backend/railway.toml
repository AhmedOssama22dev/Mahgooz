[build]
builder = "NIXPACKS"

[deploy]
releaseCommand = "python manage.py ensure_schema"
startCommand = "python manage.py ensure_schema && gunicorn config.wsgi:application --bind 0.0.0.0:${PORT:-8000}"
healthcheckPath = "/api/v1/health"
restartPolicyType = "ON_FAILURE"
